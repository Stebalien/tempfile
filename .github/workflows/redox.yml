name: Redox CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions: {}

jobs:
  redox:
    runs-on: ubuntu-latest
    container: redoxos/redoxer
    steps:
      - uses: actions/checkout@v6
      - name: Build
        run: redoxer build
      - name: Test
        run: redoxer test
